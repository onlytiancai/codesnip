# Vue 3 + TypeScript + Vite

## setup

    pnpm create vite shadcn-vue-test
    ◇  Select a framework:
    │  Vue
    │
    ◇  Select a variant:
    │  TypeScript

    cd shadcn-vue-test
    pnpm install
    pnpm dev

    pnpm add -D tailwindcss postcss autoprefixer

