import numpy as np

# 房屋面积（平方米）
area = np.array([
    50, 80, 120,     # 距离近：小 / 中 / 大
    55, 85, 130,     # 距离中：小 / 中 / 大
    60, 90, 150,     # 距离远：小 / 中 / 大
    100, 140, 180    # 距离中偏远：中 / 大 / 超大
])

# 离市中心距离（公里）
distance = np.array([
    1.5, 1.5, 1.5,   # 近
    5.0, 5.0, 5.0,   # 中
    10.0, 10.0, 10.0,# 远
    7.0, 7.0, 7.0    # 中偏远
])

# 房价（万元）
price = np.array([
    360, 520, 720,   # 近距离：面积影响明显
    300, 460, 650,   # 中距离
    260, 400, 600,   # 远距离
    500, 680, 820    # 中偏远但大面积
])